<h1>New Post for "<%= @idea.title %>"</h1>

<%= simple_form_for [@idea, @post] do |f| %>
  <%= f.input :title %>
  <%= f.input :tagline %>
  <%= f.input :summary %>

  <%= f.button :submit, "Create Post" %>
<% end %>

<%= link_to "Back to idea", edit_idea_path(@idea) %>
